apiVersion: v1
kind: Service
metadata:
  name: livraria-api
spec:
  type: NodePort
  selector:
    app: livraria-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
